using UnityEngine;
using System.Collections;

public class Richting : MonoBehaviour
{
	GameObject speler;
	Vector3 rotatie;
	float richting;

	// Use this for initialization
	void Start ()
	{
	/*	speler = GameObject.Find ("Player/FirstPersonCharacter/Pistool");
		rotatie = speler.transform.eulerAngles;


		if (rotatie.y >= -45 && rotatie.y <= 45 || rotatie.y > 135 && rotatie.y < 225)
		{
			richting = 0.0f;
		}

		else 
		{
			richting = 90.0f;
		}


		transform.eulerAngles = new Vector3 (0,richting, 0); */
	}
	
	// Update is called once per frame
	void Update () 
	{
	
	}
}
